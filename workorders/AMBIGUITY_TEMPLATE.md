AMBIGUITY REPORT   WO-###   file:function
Question (one sentence):
What the spec says / does not say (quote):
Options considered (A/B/…), and why the spec does not decide:
Impact if the wrong option is picked:
Tests blocked:
