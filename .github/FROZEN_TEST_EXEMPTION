# CONTRACT rule 2 exemption -- WO-002 authoring window ONLY.
#
# Rule 2 makes tests/unit, tests/behavioural and tests/golden read-only for implementers. WO-002 is
# the card that WRITES them, and a script cannot tell a lead authoring a frozen test from an
# implementer tampering with one, so the guard flags every such diff for review. This file is that
# review, made explicit and visible in the diff that needs it.
#
# SCOPE: exactly the files WO-002 is authoring in the current batch, and nothing else.
#
# THIS FILE MUST BE DELETED when WO-002's final batch lands. A standing entry here is a permanently
# disabled rule (scripts/README.md, "The rule-2 exemption"), which is worse than no rule at all:
# from that point on an implementer could edit these tests and CI would say nothing.
# Tracked as the last item of #7.

tests/conftest.py
tests/unit/test_rng.py
tests/unit/test_prices.py
tests/unit/test_reward.py
tests/unit/test_config.py
tests/unit/test_conservation.py
tests/unit/test_production.py
tests/unit/test_obs.py
tests/unit/test_planner.py
tests/unit/test_reporting.py
